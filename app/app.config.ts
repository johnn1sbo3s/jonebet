export default defineAppConfig({
  ui: {
    colors: {
      primary: 'teal',
      neutral: 'slate'
    },
    button: {
      base: 'cursor-pointer'
    },
    container: {
      base: 'w-full max-w-400 mx-auto px-4 sm:px-6 lg:px-8'
    }
  }
})
